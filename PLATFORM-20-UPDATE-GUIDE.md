# Platform 20 / Cash Grab v7.1

1. Back up or commit the current HoopLoop repository.
2. Copy this update folder into the repository root and replace matching files.
3. Keep the existing `config.js`.
4. Do **not** run new SQL; Cash Grab still uses the existing v5 backend.
5. Commit, push, and hard-refresh `verify.html` and `cash-grab.html`.

Expected verifier: **Platform 20 Check**.
