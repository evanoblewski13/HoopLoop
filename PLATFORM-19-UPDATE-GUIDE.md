# HoopLoop Platform 19 / Cash Grab v7 update

## Install

1. Back up or commit the current HoopLoop repository.
2. Extract `hooploop-platform-19-cash-grab-v7.zip`.
3. Copy the contents of `hooploop-cash-grab-v7-update` directly into the repository root and replace matching files.
4. Do **not** delete or replace `config.js`, Stats Race files, Name Rush data, SBC data, or HoopLoopSim files.
5. **No new SQL is required.** Cash Grab v7 keeps the existing Cash Grab v5 draft/Daily backend.
6. Commit with: `Rebuild Cash Grab around real matchup data`.
7. Push origin and wait for GitHub Pages.
8. Hard-refresh `cash-grab.html` and `verify.html` with **Ctrl + Shift + R**.

## Expected verifier

- 192 Current players
- 230 All-Time players
- 422 real player baselines
- 230 real All-Time peak-season baselines
- 33,124 observed shared-game H2H samples
- 0 synthetic player-stat fallbacks
- Cash Grab v5 backend remains installed
- Stats Race v4 / backend v3 remain intact
