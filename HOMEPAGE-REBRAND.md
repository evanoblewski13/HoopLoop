# HoopLoop Homepage — HoopLoopSim card update

Because the main HoopLoop `index.html` evolves independently from the offline simulator, Alpha 0.5 does not overwrite it.

Find the current simulator game card and change it to approximately:

```html
<h3>HoopLoopSim</h3>
<p>A customizable basketball career simulator built around player creativity and choice.</p>
<a href="hooploopsim.html">PLAY ALPHA 0.5 →</a>
```

Keep your existing card classes / surrounding HTML so it preserves the current HoopLoop homepage design.
